{
  "name": "github-hq",
  "version": "1.0.0",
  "description": "AI-powered GitHub workflow automation with MCP servers",
  "main": "scripts/agent-runner.js",
  "type": "commonjs",
  "scripts": {
    "start": "node scripts/agent-runner.js",
    "agent": "node scripts/agent-runner.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "eslint scripts/ --fix || true"
  },
  "keywords": [
    "github",
    "automation",
    "ai",
    "claude",
    "mcp",
    "workflow"
  ],
  "author": "GitHub HQ",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.16.0",
    "@modelcontextprotocol/sdk": "^0.6.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "eslint": "^8.54.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
